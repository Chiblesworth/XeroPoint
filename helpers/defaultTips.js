/*
    Since this is a constant array I can use rather than use AsyncStorage
    Tried to a similar method with the custom tips the user can set, but 
    was unable to get it to work properly, so AsyncStorage is used for that one
*/
export const defaultTips = ["No Tip", "15%", "20%", "25%"];