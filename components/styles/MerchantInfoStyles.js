import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: '5%'
    },
    merchantName: {
        fontWeight: '500',
        color: '#000',
        fontSize: 15,
        paddingBottom: '3%',
        letterSpacing: 0.5
    },
    text: {
        letterSpacing: 0.5,
        fontSize: 14,
        color: '#000'
    }
});