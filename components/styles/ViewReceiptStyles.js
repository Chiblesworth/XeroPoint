import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    headerText: {
        fontSize: 25,
        color: '#fff',
        paddingBottom: 30
    },
    receiptContainer: {
        backgroundColor: '#fff',
        height: '100%',
        margin: 20,
    },
    divider: {
        borderBottomWidth: 1,
        borderColor: '#010101',
        width: '90%',
    }
});