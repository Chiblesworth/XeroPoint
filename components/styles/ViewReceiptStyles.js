import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    receiptContainer: {
        backgroundColor: '#fff',
        height: '100%',
        margin: '8%',
    },
    divider: {
        borderBottomWidth: 1,
        borderColor: '#010101',
        width: '90%',
    }
});