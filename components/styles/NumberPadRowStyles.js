import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    row: {
        flexDirection: 'row',
    },
    padButton: {
        alignItems: 'center',
        justifyContent: 'center',
        width: 115,
        height: 110,
        color: '#fff'
    },
    buttonText: {
        color: '#fff',
        fontSize: 50
    }
});
