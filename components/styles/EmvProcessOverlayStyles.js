import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    overlay: {
        borderColor: '#2454EF',
        borderWidth: 5
    },
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        fontSize: 22,
        color: '#fff'
    }
});