import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    headerText: {
        fontSize: 25,
        color: '#fff',
        paddingBottom: 30
    },
    payment: {
        borderWidth: 1,
        borderColor: '#000',
    },
    row: {
        padding: 10,
        width: '100%',
        flexDirection: 'row',
    },
    paymentText: {
        fontSize: 18
    },
    statusContainer: {
        alignItems: 'center',
        height: 25,
        borderRadius: 5
    }
});