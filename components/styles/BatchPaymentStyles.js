import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    payment: {
        borderWidth: 1,
        borderColor: '#000',
    },
    row: {
        padding: '4%',
        width: '100%',
        flexDirection: 'row',
    },
    paymentText: {
        fontSize: 18
    },
    statusContainer: {
        alignItems: 'center',
        height: 25,
        borderRadius: 5
    }
});